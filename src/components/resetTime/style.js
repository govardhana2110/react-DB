import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  Successful: {
    color: '#5cb85c',
  },
  Failure: {
    color: 'tomato',
  },
  Response: {
    color: '#343a40',
  },
}));
export default useStyles;
