import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  titleStyle: {
    paddingRight: '10px',
    paddingBottom: '8px',
    fontSize: 13,
    color: 'black',
    fontWeight: '500',
  },
  MenuItem: {
    fontSize: 13,
  },
}));
export default useStyles;
