import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  titleStyle: {
    paddingRight: '10px',
    fontSize: 13,
    color: 'black',
    fontWeight: '500',
  },
}));
export default useStyles;
